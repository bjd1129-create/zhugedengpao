# 2026-04-06 工程日志

## P0 Bug 修复

### 【P0-1】story.html 漫画标签空壳 ✅ 已修复并推送

**问题**：`stories` JS 数组中的图片路径全是错误的（指向不存在的 `images/comic-ep1-p*.png` 和 `images/comic/storyN-*.png`），导致所有漫画标签点击后显示破图或空白。

**本地实际文件**（`images/` 目录）：
- `comic-lobster-story1-p1.jpg` ~ p8.jpg（无 -cn 后缀）
- `comic-lobster-story2-p1.jpg` ~ p8.jpg（无 -cn 后缀）
- `comic-lobster-story3~7-p1-cn.jpg` ~ p8-cn.jpg（有 -cn 后缀）
- `comic-lobster-story12-p1-cn.jpg` ~ p8-cn.jpg（有 -cn 后缀）
- **stories 8-22 不在本地（服务器文件未同步）**

**修复内容**：
1. `stories` 数组路径统一改为 `../images/comic-lobster-storyN-pX.jpg` 格式
2. stories 1-2 不加 -cn，stories 3-7 加 -cn（与实际文件名一致）
3. stories 1-7 全部正确接入（stories 8-14 待服务器同步后接入）

**待办**：stories 8-14 图片文件需从服务器同步到 `images/` 目录后，才能接入 tabs 2-7 对应的故事。Tab 标签暂时保留"故事二"-"故事七"泛指，待配色师确认实际故事标题后补充。

---

### 【P0-2】index.html DOM 结构错误 ✅ 已修复并推送

**问题**：有 1 个 `<main>` 开口（line 350）但有 2 个 `</main>` 闭合（line 571 和 line 1254）。第一个 `</main>`（line 571）过早关闭了 `<main>`，导致 HOT GUIDES / ADOPT / SCIENCE 等主要内容段落游离在 `<main>` 之外。

**修复**：删除 line 571 的 `</main>`，保留 line 1253 的 `</main>`。修改后结构：
- `<main>` 开口：line 350
- `<main>` 闭合：line 1253
- HOT GUIDES / ADOPT / SCIENCE / DIARY GRID 等全部正确包裹在 `<main>` 内

**Git Commit**: `1b04b4e` - `fix: P0-1 story.html image paths + P0-2 index.html DOM structure`

---

## 备注

- 配色师（designer）需要确认 stories 8-14 的故事标题，以便更新 tab 标签
- stories 8-14 的图片文件需从服务器同步到 `website/images/` 目录，路径格式：`comic-lobster-story{8~14}-p1.jpg` ~ p8.jpg
