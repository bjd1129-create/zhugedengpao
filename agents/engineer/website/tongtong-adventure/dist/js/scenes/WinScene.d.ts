import Phaser from 'phaser';
export declare class WinScene extends Phaser.Scene {
    private level;
    constructor();
    init(data: {
        level: number;
    }): void;
    create(): void;
    private createFireworks;
}
//# sourceMappingURL=WinScene.d.ts.map