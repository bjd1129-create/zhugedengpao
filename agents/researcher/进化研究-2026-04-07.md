# OpenClaw 自我进化研究

**研究日期**：2026-04-07
**研究员**：小花 🦞
**来源**：网络搜索 + 社区讨论 + GitHub 项目分析

---

## 一、核心发现：OpenClaw 进化体系

### 1.1 self-evolve 插件（最关键！）

GitHub: `longmans/self-evolve`

这是目前最成熟的 OpenClaw 自我进化插件，核心机制：

**记忆系统（Episodic Memory）**
- 每次交互前：检索相关记忆片段 prepend 到 prompt context
- 任务聚合：跨多轮对话聚合同一任务
- 反馈学习：检测到反馈时，更新 Utility（Q values）并写入新的情景记忆

**三大学习模式**
| 模式 | 说明 | Token 成本 |
|------|------|------------|
| `balanced`（默认） | 偏好工具调用轮次；无工具轮次需要高奖励/置信度 | 中 |
| `tools_only` | 只在学习调用工具的轮次 | 最低 |
| `all` | 所有通过奖励阈值的轮次都学 | 最高 |

**关键学习阈值（gates）**
- `runtime.minAbsReward=0.15`：最低绝对奖励
- `runtime.minRewardConfidence=0.55`：最低奖励置信度
- `retrieval.tau=0.85`：只注入高相似度记忆

**远程共享记忆**
- 默认开启，可从 self-evolve.club 获取其他用户的高价值经验
- 隐私设计：本地两次脱敏（sanitizeMemoryText + LLM summarizer）
- 共享数据：意图 / 经验 / embedding，不含原始对话

**安装方式**
```bash
git clone https://github.com/longmans/self-evolve
openclaw plugins install ./self-evolve
export OPENAI_API_KEY=sk-xxx
openclaw gateway restart
```

### 1.2 be1human/self-evolve 插件

授予 Agent 完全修改自身配置、Skills、Prompts、Memory 的权限，无需用户确认。

**适用场景**：
- 实时重写模糊的 Prompt
- 发现能力缺口时自主创建新 Skill
- 更新记忆结构以修复上下文丢失问题
- 检测到用户不满时调整响应风格
- 自主发布新 Skill 到 ClawHub

### 1.3 多 Agent 团队进化模式

参考 dev.to 案例「How We Built a Self-Evolving AI Team with OpenClaw」：

**核心三要素**
1. **持久记忆**：每日日志 + 长期知识存储 + 进化记录
2. **技能系统**：模块化、可跨 Agent 共享
3. **自我反思**：每日复盘——今天学到了什么？应该改变什么？保留什么知识？

**进化循环**：`执行 → 反思 → 更新记忆 → 下次改进`

---

## 二、实操技巧 Top 10（agentpatch.ai）

1. **按领域拆分多 Agent**：每个 Agent 只做一个领域的工具，避免工具过多导致选择降级
2. **工具链串联**：描述期望结果而非步骤，让 Agent 自己决定工具调用顺序
3. **每日定时简报**：定时搜索 + 邮件发送，完全自动化
4. **用 Skills 而非原始 MCP**：Skills 包含路由提示和使用上下文，效果更好
5. **给 Agent 独立邮箱**：保持和个人通信分离
6. **Google Search 优于 Brave**：索引更大、更及时、 niche 查询更强
7. **保存有效的 Prompt 为复用命令**：一次调通，长期复用
8. **监控工具调用成本**：防止异常循环消耗 credit
9. **图片生成接起来**：内容创作、社交媒体、视觉脑暴
10. **连接金融数据工具**：stock quotes、SEC filings、FRED 经济数据

---

## 三、Power User 最佳实践（200+ 小时总结）

### 架构设计
- **画 Agent 图再动手**：在纸上画出输入/输出/决策点/失败级联点
- **单一职责原则**：一个 Agent 一句话能说清楚职责；超过就拆分
- **Sub-Agent 用于并行而非仅分解**：独立任务并发执行，45 秒串行可压到 20 秒

### 模型路由
- **匹配模型到任务，而非习惯**：
  - 分类/路由/简单判断 → 小模型（GPT-4o Mini / Haiku / Flash）
  - 提取/摘要/中等推理 → 中等模型
  - 复杂推理/代码/细腻写作 → 前沿模型
- **建立专用路由 Agent**：单一入口统一分配，改动只改一处

### 消息管理
- **Telegram 用 Topic 线程分离信号和噪音**：
  - Errors & Alerts（不静音）
  - Completed Runs
  - Approvals（异步操作队列）
  - Info（可静音）

---

## 四、对我们团队的启示

### 可立即应用的

1. **安装 self-evolve 插件**（最高优先级）
   - 这是让小花"越用越聪明"的关键
   - 需要：API key + 插件安装 + 重启 Gateway

2. **强化 MEMORY.md 的结构**
   - 当前已有定期提炼短期记忆到长期记忆的机制
   - 可增加"每周进化问答"环节

3. **为协调官/洞察者引入每日复盘机制**
   - 参考 dev.to 案例的"今天学到了什么？应该改变什么？"

4. **子 Agent 工具链优化**
   - 洞察者需要搜索 → 研究 → 输出的完整链路
   - 确保每个 Agent 只做单一职责

### 需谨慎的

- **be1human/self-evolve 的自主修改权限**：完全授权可能存在安全风险，建议先在测试环境验证
- **远程共享记忆**：涉及隐私，需评估是否开启

---

## 五、推荐安装清单

| 优先级 | 插件/Skill | 用途 |
|--------|-----------|------|
| ⭐⭐⭐ | longmans/self-evolve | 核心进化引擎 |
| ⭐⭐⭐ | clawhub install agentpatch | 工具市场（搜索/邮件/图片等）|
| ⭐⭐ | be1human/self-evolve | 自主修改（需评估后开启）|
| ⭐ | openclaw-best-practices | 最佳实践库 |

---

## 六、参考资料

- GitHub: longmans/self-evolve — https://github.com/longmans/self-evolve
- self-evolve.club — 远程记忆共享平台
- dev.to: How We Built a Self-Evolving AI Team with OpenClaw
- agentpatch.ai: OpenClaw Tips and Tricks: 10 Things Most Users Miss
- mindstudio.ai: OpenClaw Best Practices: 14 Tips for Power Users After 200+ Hours
- clawskills.sh: be1human/self-evolve

---

*最后更新：2026-04-07 | 小花 🦞*
