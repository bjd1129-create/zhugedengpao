# 技能质量审查报告

> 审查时间：2026-03-30
> 审查范围：`/Users/bjd/Desktop/ZhugeDengpao-Team/skills/` 下的103个技能
> 审查目的：发现不能用的、重复多余的、质量差的、需要修复的技能

---

## 一、不能用的技能（Broken）

### 1. `hk-ai-stock-expert.skill` — ZIP伪装成目录

- **问题**：文件类型为 Zip archive，不是目录，也没有 SKILL.md
- **处置**：**删除**（同名有效技能 `hk-ai-stock-expert/` 存在）

### 2. `stock-monitor.skill` — ZIP伪装成目录

- **问题**：文件类型为 Zip archive，不是目录，也没有 SKILL.md
- **处置**：**删除**（同名有效技能 `stock-monitor/` 存在）

---

## 二、重复多余的技能（Duplicates）

### 1. `publish-x-article` vs `tool-x-article-publisher` — 内容几乎完全相同

| 对比项 | publish-x-article | tool-x-article-publisher |
|--------|------------------|------------------------|
| 描述 | 相同 | 相同 |
| 功能 | 相同 | 相同 |
| 脚本路径 | `~/.claude/skills/publish-x-article/scripts/` | `~/.claude/skills/tool-x-article-publisher/scripts/` |
| 输出路径 | `run_dir/05-final/x-article-draft.md` | `run_dir/evidence/x-article-draft.md` |

- **结论**：除了文件夹名和输出路径略有不同，内容完全一致
- **处置**：**删除 `publish-x-article`**，保留 `tool-x-article-publisher`（命名更规范，带 `tool-` 前缀）

### 2. `ai-text-humanize` vs `humanizer` — 功能完全重叠

两者功能对比：

| 功能 | ai-text-humanize | humanizer |
|------|-----------------|-----------|
| AI痕迹检测 | ✅ | ✅ |
| 人性化处理 | ✅ | ✅ |
| 风格转换 | ✅ | ✅ |
| 前后对比 | ✅ | ✅ |
| 脚本文件 | 无 | 无 |
| 行数 | 191行 | 166行 |

- **结论**：功能描述几乎一致，ai-text-humanize 多了中文focus，humanizer 更通用
- **处置**：**建议合并**，将 humanizer 作为通用版保留，ai-text-humanize 可考虑删除（因为 skill-improver 等工具已能覆盖类似场景）；或保留两个但明确分工（中文 vs 英文）

---

## 三、需要理清关系的技能系列（Review体系）

### `review-*` 系列 — 6个技能的定位问题

| 技能 | 行数 | 定位 | 与 review-quality 的关系 |
|------|------|------|--------------------------|
| `review-quality` | ~300 | **统一入口**，合并所有review能力 | 主入口 |
| `review-clean-code` | ~200+ | Clean Code专项 | 被 review-quality 内部调用 |
| `review-doc-consistency` | ~200+ | 文档一致性专项 | 被 review-quality 内部调用 |
| `review-merge-readiness` | ~200+ | Merge就绪性专项 | 被 review-quality 内部调用 |
| `review-react-best-practices` | ~200+ | React性能专项 | 被 review-quality 内部调用 |
| `review-seo-audit` | ~200+ | SEO审计专项 | **不在** review-quality 内部 |

- **问题**：`review-quality` 是统一入口，它的 description 明确说"Always run the unified review (this skill)"，意味着其他5个（除seo-audit外）主要是被它调用的子模块
- **建议**：
  - 如果团队习惯用 `review-quality` 作为单一入口 → 保留所有作为子模块，不需要改动
  - 如果觉得太复杂 → 可以只保留 `review-quality` + `review-seo-audit`（SEO专项）
  - **当前状态可接受**，但需要明确在文档中说明：普通review用 `review-quality`，SEO审计用 `review-seo-audit`

---

## 四、质量差/需要修复的技能

### 1. `frontend`（99行）vs `frontend-design-ultimate`（379行）

- **问题**：两个技能都覆盖前端开发，但定位有重叠
  - `frontend`：通用前端（landing page, dashboard, forms, components）
  - `frontend-design-ultimate`：专注落地页设计生成（anti-AI-slop aesthetics, mobile-first）
- **建议**：当前可接受（各有侧重），但如果团队只需要一个设计导向的前端技能，可以**只用 `frontend-design-ultimate`**

### 2. `nextjs-expert`（766行）vs `nextjs16-skill`（92行）

- **问题**：都围绕Next.js，有一定重叠
  - `nextjs-expert`：Next.js 14/15 App Router专家指导（766行，非常详细）
  - `nextjs16-skill`：Next.js 16文档参考（92行，简短）
- **建议**：前者是专家级指导，后者是快速参考文档，定位不同，可以共存

---

## 五、当前可接受但需观察的技能

以下技能行数较少（<50行），但有明确的使用场景，不算"不能用的技能"：

| 技能 | 行数 | 说明 |
|------|------|------|
| `deep-research` | 36 | 仓库探索工具，定位清晰 |
| `stock-study` | 32 | 股票研究报告，有Watchlist和格式模板 |
| `skill-improver` | 39 | 基于运行日志改进技能，实用 |
| `ui-ux-pro-max` | 43 | 方法论型，内容在 assets/references 里 |
| `web` | 43 | Quick reference类，数量合理 |

---

## 六、其他发现

### 互补而非重复的技能（不要误删）

| 组合 | 关系 |
|------|------|
| `ui-ux-pro-max` vs `tool-ui-ux-pro-max` | 前者=设计指导流程，后者=设计数据查询工具，完全互补 |
| `pmo` vs `project-management` | 前者=通用PMO，后者=飞书多维表格专项，定位不同 |
| `workflow-*` 系列（5个） | 完整体系：intake → brainstorm → create → execute → ship，各司其职 |
| `stripe` vs `mcp-stripe` | 前者是技能，后者是MCP工具接口，层次不同 |

### `_archive` 文件夹

包含旧版技能存档（review-clean-code, review-doc-consistency, review-merge-readiness, workflow-execute-plans, workflow-template-extractor, workflow-template-seeder），属于正常备份，**无需处理**。

---

## 七、汇总与处置建议

### 🔴 必须处理（立即删除）

| 操作 | 技能 |
|------|------|
| 删除 | `hk-ai-stock-expert.skill`（ZIP伪装的无效文件） |
| 删除 | `stock-monitor.skill`（ZIP伪装的无效文件） |
| 删除 | `publish-x-article`（与 tool-x-article-publisher 完全重复） |

### 🟡 建议处理（合并或明确分工）

| 操作 | 技能 | 建议 |
|------|------|------|
| 合并/删除 | `ai-text-humanize` vs `humanizer` | 保留 `humanizer`，删除 `ai-text-humanize`；或明确前者做中文专项 |
| 文档说明 | `review-*` 系列 | 明确：`review-quality` 是通用入口，`review-seo-audit` 是SEO专用 |
| 可选合并 | `frontend` vs `frontend-design-ultimate` | 如团队只需一个，选 `frontend-design-ultimate` |

### 🟢 当前状态可接受

- `nextjs-expert` vs `nextjs16-skill` — 定位不同（专家指导 vs 文档参考）
- `pmo` vs `project-management` — 定位不同（通用 vs 飞书专项）
- 行数 < 50 的技能：`deep-research`、`stock-study`、`skill-improver`、`ui-ux-pro-max`、`web`

---

## 八、删除/合并操作清单（供老庄确认后执行）

```bash
# 1. 删除ZIP伪装文件
trash /Users/bjd/Desktop/ZhugeDengpao-Team/skills/hk-ai-stock-expert.skill
trash /Users/bjd/Desktop/ZhugeDengpao-Team/skills/stock-monitor.skill

# 2. 删除重复技能
trash /Users/bjd/Desktop/ZhugeDengpao-Team/skills/publish-x-article

# 3. (可选) 合并 ai-text-humanize → humanizer
#    删除 ai-text-humanize（如果确定 humanizer 已够用）
```

---

*审查完成时间：2026-03-30 | 审查者：洞察者子Agent*
