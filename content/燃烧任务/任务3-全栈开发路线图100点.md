# 全栈开发路线图 (100个知识点)

| 序号 | 知识点名称 | 难度等级 | 学习资源链接 | 详细原理解释 | 代码示例 |
|------|----------|----------|------------|-------------|-------------|---------|
| 1 | HTML基础标签 | ⭐ | [MDN HTML教程](https://developer.mozilla.org/zh-CN/docs/Web/HTML) | HTML是超文本标记语言，用于构建网页结构。常见标签：div、span、p、a、img、ul、ol、li、table、form、input、button。标签有开始和结束标签，或自闭合标签。 | `<div><h1>标题</h1><p>段落</p></div>` |
| 2 | HTML语义化标签 | ⭐⭐ | [语义HTML](https://web.dev/articles/semantic-html) | 语义化标签(header、nav、main、section、article、aside、footer)让代码更有意义，便于搜索引擎爬虫理解和无障碍访问。代替大量div嵌套。 | `<header><nav></nav></header><main><article></article></main>` |
| 3 | CSS选择器 | ⭐ | [CSS选择器](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_Selectors) | CSS选择器用于选中HTML元素应用样式。类型：元素选择器(p)、类选择器(.class)、ID选择器(#id)、属性选择器([type=text])、伪类(:hover)、伪元素(::before)。 | `div.class#id:hover { color: red; }` |
| 4 | CSS盒模型 | ⭐⭐ | [盒模型详解](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_Box_Model) | 盒模型由content(内容)、padding(内边距)、border(边框)、margin(外边距)组成。box-sizing: border-box让width包含padding和border。 | `.box { box-sizing: border-box; width: 200px; padding: 20px; border: 1px solid #000; }` |
| 5 | CSS Flex布局 | ⭐⭐ | [Flexbox完全指南](https://css-tricks.com/snippets/css/a-guide-to-flexbox/) | Flex弹性盒子用于一维布局。核心属性：display: flex、flex-direction(方向)、justify-content(主轴对齐)、align-items(交叉轴对齐)、flex-wrap(换行)、flex-grow(放大)、flex-shrink(缩小)。 | `.container { display: flex; justify-content: space-between; align-items: center; }` |
| 6 | CSS Grid布局 | ⭐⭐⭐ | [Grid完全指南](https://css-tricks.com/snippets/css/reference/grid/) | Grid网格布局用于二维布局。核心属性：display: grid、grid-template-columns(列宽)、grid-template-rows(行高)、grid-gap(间距)、grid-area(区域)、justify-items/align-items。 | `.grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }` |
| 7 | CSS响应式设计 | ⭐⭐ | [响应式设计](https://web.dev/articles/responsive-web-design) | 通过@media查询实现不同屏幕适配。常见断点：mobile(<768px)、tablet(768-1024px)、desktop(>1024px)。使用相对单位rem、em、vw、vh。 | `@media (max-width: 768px) { .col { width: 100%; } }` |
| 8 | CSS动画与过渡 | ⭐⭐ | [CSS动画](https://developer.mozilla.org/zh-CN/docs/Web/CSS/CSS_Animations) | transition过渡控制属性变化，@keyframes定义关键帧动画。属性：animation-name、duration、timing-function、delay、iteration-count。transform实现变换(translate/rotate/scale/skew)。 | `.btn { transition: all 0.3s ease; } .btn:hover { transform: scale(1.1); }` |
| 9 | JavaScript基础语法 | ⭐ | [JS教程](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide) | 变量声明(var/let/const)、数据类型(number/string/boolean/null/undefined)、运算符、流程控制(if/for/while/switch)、函数、数组、对象。ES6+新特性：箭头函数、模板字符串、解构赋值。 | `const add = (a, b) => a + b; console.log(\`2+3=\${add(2,3)}\`);` |
| 10 | JavaScript函数 | ⭐⭐ | [函数详解](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Functions) | 函数是代码块，可重复调用。定义方式：函数声明、函数表达式、箭头函数。参数：默认参数、剩余参数、arguments对象。作用域：全局/局部/块级。闭包：函数访问外部变量。 | `function greet(name = 'World') { return \`Hello, \${name}!\`; } const add = (a, b) => a + b;` |
| 11 | JavaScript数组方法 | ⭐⭐ | [数组API](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Array) | 数组方法：map(映射)、filter(过滤)、reduce(归约)、forEach(遍历)、some/every(判断)、find/findIndex(查找)、sort(排序)、slice/spice(截取)、concat(合并)、join(连接)。 | `const nums = [1,2,3,4]; const evens = nums.filter(n => n%2===0); const sum = nums.reduce((a,b)=>a+b,0);` |
| 12 | JavaScript DOM操作 | ⭐⭐ | [DOM教程](https://developer.mozilla.org/zh-CN/docs/Web/API/Document_Object_Model) | DOM是文档对象模型。常用API：querySelector/querySelectorAll(选择)、createElement(创建)、appendChild/prepend(添加)、innerHTML/textContent(内容)、classList(类名)、style(样式)。 | `const el = document.querySelector('.box'); el.classList.add('active'); el.innerHTML = '<span>新内容</span>';` |
| 13 | JavaScript事件 | ⭐⭐ | [事件处理](https://developer.mozilla.org/zh-CN/docs/Web/Events) | 事件驱动编程。常用事件：click、input、change、keydown/keyup、submit、load、scroll、resize。事件处理：addEventListener(绑定)、removeEventListener(解绑)、event.preventDefault/stopPropagation。 | `btn.addEventListener('click', (e) => { e.preventDefault(); console.log('clicked'); });` |
| 14 | JavaScript异步编程 | ⭐⭐⭐ | [异步JS](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Guide/Using_promises) | 异步编程：回调函数、Promise、async/await。Promise状态：pending/fulfilled/rejected。Promise方法：then/catch/finally、Promise.all/race/allSettled。async声明异步函数，await等待Promise。 | `async function fetchData() { try { const res = await fetch(url); return await res.json(); } catch(e) { console.error(e); } }` |
| 15 | TypeScript基础 | ⭐⭐ | [TypeScript教程](https://www.typescriptlang.org/docs/) | TypeScript是JS的超集，添加类型系统。基本类型：string/number/boolean/数组/元组/对象/Any/Void/Never/Enum。接口interface、类型别名type、泛型generic、类型断言as。 | `interface User { id: number; name: string; email?: string; } const user: User = { id: 1, name: 'Tom' };` |
| 16 | TypeScript泛型 | ⭐⭐⭐ | [泛型详解](https://www.typescriptlang.org/docs/handbook/2/Generics.html) | 泛型提供可重用的类型参数。用<T>表示，可在函数/接口/类中使用。约束extends、多个泛型参数、默认类型。实现类似模板的功能，但编译时检查。 | `function identity<T>(arg: T): T { return arg; } interface Response<T> { code: number; data: T; }` |
| 17 | Node.js基础 | ⭐⭐ | [Node.js教程](https://nodejs.org/docs/latest/api/) | Node.js是JS运行时。核心模块：fs(文件系统)、path(路径)、http(网络)、url(URL解析)、querystring(查询字符串)、crypto(加密)、stream(流)、events(事件)。 | `const fs = require('fs'); fs.readFile('a.txt', 'utf8', (err, data) => { if(err) throw err; console.log(data); });` |
| 18 | Node.js模块系统 | ⭐⭐ | [模块系统](https://nodejs.org/docs/latest/api/modules.html) | Node.js使用CommonJS模块系统。module.exports/exports导出，require导入。ES Module：import/export。__dirname、__filename、module、exports、require内置对象。包管理：npm/yarn/pnpm。 | `// a.js module.exports = { fn: () => 'hello' }; // b.js const a = require('./a'); console.log(a.fn());` |
| 19 | Express框架 | ⭐⭐ | [Express文档](https://expressjs.com/) | Express是轻量级Web框架。核心概念：app(应用)、request(请求)、response(响应)、next(下一个中间件)、router(路由)、middleware(中间件)。常用方法：get/post/put/delete、use、set。 | `const express = require('express'); const app = express(); app.get('/api/user/:id', (req, res) => { res.json({ id: req.params.id }); }); app.listen(3000);` |
| 20 | Express中间件 | ⭐⭐⭐ | [中间件](https://expressjs.com/en/guide/using-middleware.html) | 中间件是函���链��请求经过每个中间件处理。类型：应用级(app.use)、路由级(router.use)、错误处理(err参数)、内置(body-parser)、第三方(cors/helmet)。 | `app.use((req, res, next) => { console.log(req.method, req.url); next(); }); app.use('/api', authMiddleware);` |
| 21 | RESTful API设计 | ⭐⭐⭐ | [REST设计](https://restfulapi.net/) | REST API设计规范。URL表示资源(名词复数)，HTTP方法表示操作(GET查询/POST创建/PUT更新/DELETE删除)。状态码：200成功/201创建/400客户端错误/401未授权/404未找到/500服务器错误。 | `GET /users - 获取用户列表；POST /users - 创建用户；GET /users/:id - 获取单个用户；PUT /users/:id - 更新用户；DELETE /users/:id - 删除用户` |
| 22 | HTTP/HTTPS协议 | ⭐⭐ | [HTTP协议](https://developer.mozilla.org/zh-CN/docs/Web/HTTP) | HTTP是超文本传输协议。请求：method、url、headers、body。响应：status、headers、body。版本：HTTP/1.1(持久连接)、HTTP/2(多路复用)、HTTP/3(QUIC)。HTTPS添加TLS/SSL加密。 | `GET /api/users HTTP/1.1\nHost: example.com\nAccept: application/json` |
| 23 | Cookie与Session | ⭐⭐ | [认证机制](https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Cookies) | Cookie存储在客户端，用于会话跟踪。属性：name、value、expires、domain、path、secure、httpOnly、sameSite。Session存储在服务端，通过sessionId关联。JWT是无状态的令牌。 | `// 设置cookie res.cookie('token', token, { httpOnly: true, maxAge: 3600000 }); // session app.use(session({ secret: 'key', cookie: { maxAge: 3600000 } }));` |
| 24 | 认证与授权 | ⭐⭐⭐ | [认证授权](https://owasp.org/www-project-top-ten/) | 认证确认用户身份(登录)，授权决定用户权限。常见方式：Basic、Digest、Bearer(JWT)、OAuth2、API Key。密码存储：哈希加盐(bcrypt)。双因素认证2FA。 | `// JWT const token = jwt.sign({ userId }, secret, { expiresIn: '1h' }); // Bearer res.setHeader('Authorization', \`Bearer \${token}\`);` |
| 25 | JWT令牌 | ⭐⭐⭐ | [JWT详解](https://jwt.io/introduction) | JWT(JSON Web Token)是无状态认证。组成：header.payload.signature。算法：HS256、RS256。使用场景：API认证、SSO。单点登录：refresh token机制。 | `const jwt = require('jsonwebtoken'); const token = jwt.sign({ userId: 1, role: 'admin' }, 'secret', { expiresIn: '1h' }); jwt.verify(token, 'secret');` |
| 26 | 数据库基础 | ⭐⭐ | [数据库概念](https://www.mongodb.com/basics/databases-explained) | 数据库存储结构化数据。类型：关系型(MySQL/PostgreSQL)、非关系型(MongoDB/Redis)、图数据库(Neo4j)。核心概念：database、collection(table)、document(row)、field(column)、index。 | `// MongoDB use mydb; db.users.insertOne({ name: 'Tom', age: 25 }); db.users.find({ age: { $gt: 20 } });` |
| 27 | SQL基础查询 | ⭐⭐ | [SQL教程](https://www.w3schools.com/sql/) | SQL是结构化查询语言。DDL：CREATE/ALTER/DROP。DML：SELECT/INSERT/UPDATE/DELETE。DCL：GRANT/REVOKE。子查询、JOIN(INNER/LEFT/RIGHT/FULL)、聚合(SUM/AVG/COUNT/MAX/MIN)、GROUP BY、HAVING。 | `SELECT name, age FROM users WHERE age > 20 ORDER BY age DESC LIMIT 10; SELECT u.name, o.total FROM users u LEFT JOIN orders o ON u.id = o.user_id;` |
| 28 | MongoDB基础 | ⭐⭐ | [MongoDB文档](https://www.mongodb.com/docs/) | MongoDB是文档型数据库。JSON风格文档。常用操作：insertOne/find/updateOne/deleteOne。操作符：$eq/$ne/$gt/$gte/$lt/$lte/$in/$nin/$and/$or/$not/$regex。聚合管道：$match/$group/$project/$sort/$limit。 | `db.users.find({ age: { $gte: 18 } }, { name: 1, email: 1 }).sort({ name: 1 }); db.users.aggregate([{ $match: { status: 'active' } }, { $group: { _id: '$city', count: { $sum: 1 } } }]);` |
| 29 | Redis缓存 | ⭐⭐⭐ | [Redis文档](https://redis.io/docs/) | Redis是内存键值数据库。数据类型：string、list、set、sorted set、hash、bitmap、hyperloglog。用途：缓存、会话、消息队列、限流、分布���锁���计数器。持久化：RDB/AOF。 | `SET user:1:name Tom GET user:1:name INCR page:view:home LPUSH queue:task '{"action":"email"}' LRANGE queue:task 0 -1` |
| 30 | ORM数据访问 | ⭐⭐⭐ | [Prisma文档](https://www.prisma.io/docs/) | ORM(Object-Relational Mapping)对象关系映射。Prisma/ Sequelize/ TypeORM。使用schema定义模型，自动生成迁移。优点：类型安全、可读性高、数据库无关。 | `// Prisma schema generator client { provider = "prisma-client-js" } model User { id Int @id @default(autoincrement()) name String email String @unique }` |
| 31 | Git版本控制 | ⭐ | [Git教程](https://git-scm.com/docs) | Git是分布式版本控制系统。核心概念：repository、commit、branch、tag、stash。工作流：add、commit、push、pull、fetch、merge、rebase。状态：modified、staged、committed。 | `git init git add . git commit -m 'init' git branch feature git checkout -b dev git merge feature` |
| 32 | Git分支策略 | ⭐⭐ | [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/) | 常见分支模型：Git Flow(main/master、develop、feature、release、hotfix)、GitHub Flow(main+feature)、Trunk-Based Development。命名规范：feature/、bugfix/、hotfix/。 | `git checkout -b feature/login git commit --amend git rebase -i HEAD~3 git merge --no-ff develop` |
| 33 | Docker容器 | ⭐⭐⭐ | [Docker文档](https://docs.docker.com/) | Docker容器化。镜像：Dockerfile构建。容器：镜像运行实例。命令：build、run、stop、rm、ps、logs、exec。Volumes数据卷。网络。Docker Compose多容器编排。 | `docker build -t myapp . docker run -d -p 8080:80 --name myapp myapp docker-compose up -d` |
| 34 | Kubernetes基础 | ⭐⭐⭐⭐ | [K8s文档](https://kubernetes.io/docs/) | K8s容器编排。核心概念：Pod(最小单位)、Deployment(无状态)、StatefulSet(有状态)、Service(网络)、ConfigMap/Secret(配置)、Ingress(外网访问)、HorizontalPodAutoscaler(自动扩缩)。 | `kubectl apply -f deployment.yaml kubectl get pods kubectl logs -f podname kubectl scale deployment/app --replicas=3` |
| 35 | CI/CD流水线 | ⭐⭐⭐ | [CI/CD概念](https://about.gitlab.com/features/ci-cd/) | CI持续集成/CD持续部署。工具：Jenkins、GitHub Actions、GitLab CI、CircleCI。流程：代码提交->构建->测试->部署。阶段：checkout、install、build、test、deploy。 | `stages: - build - test - deploy build: npm install npm run build test: npm test deploy: script: - kubectl apply -f deployment.yaml` |
| 36 | Linux基础命令 | ⭐ | [Linux命令](https://linux.die.net/man/) | Linux基础：文件(ls/cd/mkdir/rm/cp/mv/touch/cat/head/tail/grep/find/chmod/chown)、文本处理(wc/cut/sort/uniq/awk/sed)、进程(ps/top/kill)、网络(ss/netstat/ping/curl/wget)、归档(tar/zip)。 | `ls -la grep -r 'pattern' find . -name '*.js' ps aux \| grep node tar -czf backup.tar.gz dir/` |
| 37 | Shell脚本编程 | ⭐⭐ | [Bash教程](https://www.gnu.org/software/bash/manual/) | Shell脚本编程。变量：name=value、$name、${name}。参数：$1-$9、$@、$#、$?。控制：if/case/for/while。函数。管道|、重定向>、<。定时任务crontab。 | `#!/bin/bash for f in *.js; do echo "File: $f"; done if [ -d "$dir" ]; then cd "$dir"; fi` |
| 38 | 网络协议栈 | ⭐⭐⭐ | [网络模型](https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Overview) | TCP/IP五层模型：物理层->数据链路层->网络层(IP)->传输层(TCP/UDP)->应用层(HTTP/DNS/FTP)。TCP三次握手/四次挥手。UDP无连接。DNS解析。IP地址/CIDR。子网掩码。NAT。 | `TCP三次握手：SYN->SYN/ACK->ACK；HTTP请求：Request Line/Headers/Body；DNS查询：A/AAAA/CNAME/MX` |
| 39 | WebSocket通信 | ⭐⭐⭐ | [WebSocket](https://developer.mozilla.org/zh-CN/docs/Web/API/WebSocket) | WebSocket全双工通信。HTTP升级。握手：Sec-WebSocket-Key。帧：text/binary。状态：OPEN/CLOSING/CLOSED。心跳ping/pong。方法：send/close。库：socket.io提供自动重连/房间/广播。 | `// 服务端 const wss = new WebSocket.Server({ port: 8080 }); wss.on('connection', ws => { ws.on('message', msg => ws.send(msg)); }); // 客户端 const ws = new WebSocket('ws://localhost:8080'); ws.onmessage = e => console.log(e.data);` |
| 40 | GraphQL API | ⭐⭐⭐⭐ | [GraphQL](https://graphql.org/learn/) | GraphQL查询语言。类型系统：Scalar/Object/Input/Enum/Interface/Union。Schema定义。Query查询/Mutation变更/Subscription订阅。解析器resolver。工具：Apollo Server/Client。vs REST：一次请求获取需要的数据。 | `type Query { user(id: ID!): User users: [User]! } type Mutation { createUser(input: UserInput!): User } query { user(id: "1") { name posts { title } } }` |
| 41 | Vue.js基础 | ⭐⭐ | [Vue3文档](https://vuejs.org/) | Vue.js渐进式框架。响应式：ref/reactive/computed/watch。指令：v-if/v-for/v-model/v-bind/:class/:style。组件：props/emits/slots。生命周期：onMounted/onUpdated/onUnmounted。组合式APIsetup。 | `<script setup> import { ref, computed } from 'vue'; const count = ref(0); const double = computed(() => count.value * 2); </script> <template><button @click="count++">{{ count }} x 2 = {{ double }}</button></template>` |
| 42 | Vue.js组件通信 | ⭐⭐⭐ | [组件通信](https://vuejs.org/guide/components/provide-inject.html) | 组件通信方式。Props：父->子。Emits：子->父。Provide/Inject：祖先->后代。Ref：获取组件实例。Pinia/Vuex：全局状态。Event Bus：mitt。Provide比Inject优先。 | `// 父 <Child :msg="msg" @event="handler" /> // 子 props: { msg: String }; emits: ['event']; // Inject provide: { theme: ref('dark') }; const theme = inject('theme');` |
| 43 | Vue Router路由 | ⭐⭐ | [Vue Router](https://router.vuejs.org/) | Vue Router单页应用路由。定义：createRouter/createWebHistory。路径：path/name。参数：/:id。守卫：beforeEach/beforeEnter。嵌套路由：children。滚动行为：scrollBehavior。动态路由：addRoute。 | `const routes = [{ path: '/user/:id', name: 'user', component: User, children: [{ path: 'profile', component: UserProfile }] }]; router.beforeEach((to, from) => { if(to.meta.requiresAuth) return '/login'; });` |
| 44 | Pinia状态管理 | ⭐⭐⭐ | [Pinia文档](https://pinia.vuejs.org/) | Pinia Vue3推荐状态管理。Store定义：defineStore。state响应式。getters计算属性。actions异步方法。$subscribe监听变化。插件持久化。模块化：defineStore的id。 | `export const useStore = defineStore('main', { state: () => ({ count: 0 }), getters: { double: state => state.count * 2 }, actions: { increment() { this.count++; } } });` |
| 45 | React基础 | ⭐⭐ | [React文档](https://react.dev/) | React声明式UI库。函数组件。JSX语法。Props只读。State响应式。Hooks：useState/useEffect/useRef/useCallback/useMemo/useContext。条件渲染/列表渲染。表单受控组件。 | `function App() { const [count, setCount] = useState(0); return <div><h1>{count}</h1><button onClick={()=>setCount(c=>c+1)}>+</button></div>; }` |
| 46 | React Hooks深入 | ⭐⭐⭐ | [Hooks API](https://react.dev/reference/react) | React Hooks高级。useEffect：副作用(数据获取/订阅/定时器)，依赖数组[deps]，返回清理函数。useRef：可变引用，不触发重渲染。useCallback：记忆化函数。useMemo：记忆化值。useReducer：复杂状态逻辑。自定义Hooks。 | `useEffect(() => { const sub = api.subscribe(id); return () => sub.unsubscribe(); }, [id]); const memo = useMemo(() => expensive(a, b), [a, b]);` |
| 47 | React Router路由 | ⭐⭐ | [React Router](https://reactrouter.com/) | React Router路由。Routes/Route定义。useNavigate编程导航。useParams/useSearchParams获取参数。Navigate重定向。Outlet嵌套路由。Lazy加载：React.lazy/Suspense。 | `<Routes><Route path="/user/:id" element={<User />} /><Route path="*" element={<NotFound />} /></Routes> const { id } = useParams(); const navigate = useNavigate();` |
| 48 | Redux状态管理 | ⭐⭐⭐⭐ | [Redux Toolkit](https://redux.js.org/) | Redux状态管理。Store单一可信源。Actions：type/payload。Reducers：纯函数。Dispatch派发。Selectors选择数据。Redux Toolkit简化：createSlice/createAsyncThunk。中间件：redux-thunk/saga。持久化。 | `const slice = createSlice({ name: 'counter', initialState: { value: 0 }, reducers: { increment: state => { state.value += 1 } } }); store.dispatch(counterSlice.actions.increment());` |
| 49 | Next.js全栈框架 | ⭐⭐⭐⭐ | [Next.js文档](https://nextjs.org/docs) | Next.js React全栈框架。App Router：app目录。Server Components默认。路由：page.tsx/layout.tsx。数据获取：async/await。API Routes：route.ts。Server Actions：'use server'。中间件。ISR/SSG/SSR。 | `// app/page.tsx export default async function Page() { const data = await fetchData(); return <main>{data.title}</main>; } // app/api/route.ts export async function GET() { return Response.json({ data }); }` |
| 50 | Tailwind CSS | ⭐⭐ | [Tailwind文档](https://tailwindcss.com/docs/) | Tailwind原子化CSS框架。实用优先的类名。响应式前缀：md:/lg:。变体：hover:/focus:。深色模式：dark:。自定义配置：tailwind.config.js。@apply指令提取。JIT引擎。 | `<div class="flex items-center justify-between p-4 bg-white dark:bg-slate-900"><h1 class="text-2xl font-bold">Title</h1><button class="px-4 py-2 bg-blue-500 hover:bg-blue-600 rounded">Click</button></div>` |
| 51 | 数据结构-数组 | ⭐ | [数组](https://en.wikipedia.org/wiki/Array_(data_structure)) | 数组是最基本的线性表。连续内存存储。随机访问O(1)。插入/删除O(n)。静态数组大小固定，动态数组可扩容。JavaScript中Array是动态数组。操作：push/pop/shift/unshift/splice。 | `const arr = [1,2,3]; arr.push(4); // O(1) arr.splice(1,0,5); // O(n) for(let i=0;i<arr.length;i++) console.log(arr[i]);` |
| 52 | 数据结构-链表 | ⭐⭐ | [链表](https://en.wikipedia.org/wiki/Linked_list) | 链表节点包含数据和指向下一节点的指针。单链表/双链表/循��链��。插入/删除O(1)，查找O(n)。不需要连续内存。队列和栈常用链表实现。JavaScript可以用Object模拟。 | `class ListNode { constructor(val) { this.val = val; this.next = null; } } function insert(head, val) { const node = new ListNode(val); node.next = head; return node; }` |
| 53 | 数据结构-栈与队列 | ⭐⭐ | [栈队列](https://en.wikipedia.org/wiki/Stack_(abstract_data_type)) | 栈：LIFO后进先出。push/pop/top。队列：FIFO先进先出。enqueue/dequeue。优先队列：优先级高先出。双端队列deque。JS中数组可模拟栈，push/pop。队列用指针实现或使用TypedArray。 | `// 栈 const stack = []; stack.push(1); stack.pop(); // 队列 const queue = []; queue.push(1); queue.shift();` |
| 54 | 数据结构-树 | ⭐⭐⭐ | [树](https://en.wikipedia.org/wiki/Tree_(data_structure)) | 树是层次结构。二叉树最多两个子节点。BST二叉搜索树查找O(log n)。遍历：前序/中序/后序/层序。红黑树/AVL树自平衡。B-tree用于数据库。Tries用于前缀匹配。 | `class TreeNode { constructor(val) { this.val = val; this.left = null; this.right = null; } } // 中序遍历 function inorder(node) { if(!node) return; inorder(node.left); visit(node.val); inorder(node.right); }` |
| 55 | 数据结构-图 | ⭐⭐⭐⭐ | [图](https://en.wikipedia.org/wiki/Graph_(abstract_data_type)) | 图由顶点和边组成。表示：邻接矩阵/邻接表。遍历：BFS广度优先/DFS深度优先。最短路径：Dijkstra/Bellman-Ford/Floyd。最小生成树：Prim/Kruskal。网络流。 | `// BFS function bfs(graph, start) { const visited = new Set(); const queue = [start]; while(queue.length) { const v = queue.shift(); if(!visited.has(v)) { visited.add(v); queue.push(...graph[v]); } } }` |
| 56 | 算法-排序 | ⭐⭐ | [排序算法](https://www.geeksforgeeks.org/sorting-algorithms/) | 常见排序：冒泡O(n²)、选择O(n²)���插入O(n²)、归并O(n log n)、快速O(n log n)、堆O(n log n)、计数O(n+k)、基数O(nk)、桶O(n+k)。稳定性。原地排序。 | `// 快排 function quickSort(arr, l=0, r=arr.length-1) { if(l>=r) return; const p = partition(arr, l, r); quickSort(arr, l, p-1); quickSort(arr, p+1, r); }` |
| 57 | 算法-搜索 | ⭐⭐ | [搜索算法](https://www.geeksforgeeks.org/searching/) | 搜索算法：线性O(n)、二分O(log n)、深度优先DFS、广度优先BFS。哈希查找O(1)。字符串匹配：KMP/Boyer-Moore。DFS/BFS用于图/树遍历、迷宫、连通分量。 | `// 二分 function binarySearch(arr, target) { let l=0, r=arr.length-1; while(l<=r) { const m=(l+r)>>1; if(arr[m]===target) return m; if(arr[m]<target) l=m+1; else r=m-1; } return -1; }` |
| 58 | 算法-动态规划 | ⭐⭐⭐⭐ | [动态规划](https://www.geeksforgeeks.org/dynamic-programming/) | 动态规划DP解决最优子结构问题。步骤：定义状态、推导方程、初始化、计算。自底向上(填表)或自顶向下(记忆化)。经典问题：斐波那契/背包/最短路径/LIS。 | `// 斐波那契 function fib(n) { const dp = new Array(n+1).fill(0); dp[1] = 1; for(let i=2;i<=n;i++) dp[i] = dp[i-1] + dp[i-2]; return dp[n]; } // 记忆化 function fib(n, memo={}) { if(n in memo) return memo[n]; if(n<=1) return n; return memo[n] = fib(n-1)+fib(n-2); }` |
| 59 | 算法-贪心 | ⭐⭐⭐ | [贪心算法](https://www.geeksforgeeks.org/greedy-algorithms/) | 贪心每步选择局部最优。适用：最优子结构+贪心选择性质。经典问题：活动选择、哈夫曼编码、最小生成树(Prim/Kruskal)、Dijkstra。单源最短路径。并非总是最优。 | `// 活动选择 function activitySelection(activities) { activities.sort((a,b)=>a.end-b.end); let count=1, lastEnd=activities[0].end; for(let i=1;i<activities.length;i++) { if(activities[i].start>=lastEnd) { count++; lastEnd=activities[i].end; } } return count; }` |
| 60 | 算法-回溯 | ⭐⭐⭐⭐ | [回溯算法](https://www.geeksforgeeks.org/backtracking-algorithms/) | 回溯递归搜索+撤销选择。适用：穷举搜索。经典问题：N皇后、组合求和、子集、排列、解数独、单词搜索。全排列/子集生成。时间复杂度指数级。剪枝优化。 | `// 全排列 function permute(nums, start) { if(start===nums.length) { result.push([...nums]); return; } for(let i=start;i<nums.length;i++) { [nums[start], nums[i]] = [nums[i], nums[start]]; permute(nums, start+1); [nums[start], nums[i]] = [nums[i], nums[start]]; } }` |
| 61 | 算法-分治 | ⭐⭐⭐ | [分治](https://www.geeksforgeeks.org/divide-and-conquer/) | 分治将大问题分解为小问题。步骤：分解、解决、合并。经典：归并排序、快速排序、逆序对、最近点对、大整数乘法。MapReduce也是分治思想。 | `// 归并排序 function mergeSort(arr) { if(arr.length<=1) return arr; const m = arr.length>>1; const left = mergeSort(arr.slice(0,m)); const right = mergeSort(arr.slice(m)); return merge(left, right); }` |
| 62 | 时间空间复杂度 | ⭐⭐ | [复杂度](https://www.bigocheatsheet.com/) | 复杂度分析。大O表示法：O(1)常数、O(logn)对数、O(n)线性、O(nlogn)线性对数、O(n²)平方、O(2^n)指数。空间复杂度：额外内存。最好/平均/最坏情况。 | `// O(n) for(let i=0;i<n;i++) // O(n²) for(let i=0;i<n;i++)for(let j=0;j<n;j++) // O(logn) while(n>1)n>>=1` |
| 63 | 前端性能优化 | ⭐⭐⭐ | [性能优化](https://web.dev/articles/fast) | 前端性能：减少渲染阻塞( async/defer)、代码分割(Code Splitting)、懒加载、Tree Shaking、压缩、缓存、雪碧图、CDN、预加载preload/prefetch。Web Vitals：LCP/FID/CLS。 | `<script defer src="app.js"></script> <link rel="preload" href="font.woff2" as="font" crossorigin> const img = new Image(); img.src = 'lazy.jpg'; img.loading = 'lazy';` |
| 64 | 后端性能优化 | ⭐⭐⭐ | [后端优化](https://engineering.fb.com/) | 后端性能：数据库索引、查询优化(N+1问题)��缓��(Redis)、连接池、异步处理、队列削峰、读写分离、分库分表、代码优化(算法/循环)、GC调优。Profiling定位瓶颈。 | `// 索引 CREATE INDEX idx_user_email ON users(email); // 查询优化 const users = await User.find().select('name email'); // N+1优化 const users = await User.find().populate('posts'); // 缓存 const cached = await redis.get(key); if(cached) return cached;` |
| 65 | 安全性-XSS | ⭐⭐⭐ | [XSS](https://owasp.org/www-community/attacks/xss/) | XSS跨站脚本。类型：反射型(URL参数)、存储型(数据库)、DOM型。防护：转义输出(innerText/textContent)、Content Security Policy、httpOnly Cookie、输入验证。React默认防护。 | `// 转义 function escape(str) { return str.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#x27;').replace(/\//g,'&#x2F;'); } // CSP header Content-Security-Policy: default-src 'self'` |
| 66 | 安全性-CSRF | ⭐⭐⭐ | [CSRF](https://owasp.org/www-community/attacks/csrf) | CSRF跨站请求伪造。原理：带Cookie访问恶意站点自动发送认证请求。防护：SameSite Cookie、CSRF Token、Referer检查、Origin检查、双重提交。 | `// CSRF Token csrfToken = crypto.randomBytes(32).toString('hex'); res.cookie('csrf', csrfToken, { httpOnly: true, sameSite: 'strict' }); // 请求携带 <input type="hidden" name="_csrf" value="${csrfToken}"> // 验证 if(req.body._csrf !== req.cookies.csrf) return 403;` |
| 67 | 安全性-SQL注入 | ⭐⭐⭐ | [SQL注入](https://owasp.org/www-community/attacks/SQL_Injection) | SQL注入通过输入执行恶意SQL。使用参数化查询/预编译语句。不使用字符串拼接。输入验证。白名单。最小权限原则。 | `// 正确 const result = await db.query('SELECT * FROM users WHERE id = $1', [id]); // 错误 const result = await db.query('SELECT * FROM users WHERE id = ' + id);` |
| 68 | 安全性-加密 | ⭐⭐⭐ | [加密](https://cryptography.io/) | 加密：对称(AES)、非对称(RSA)、哈希(SHA256)、签名(HMAC)。传输加密TLS。密码存储：bcrypt/argon2加盐哈希。JWT签名。密钥管理。 | `const crypto = require('crypto'); const hash = crypto.createHash('sha256').update(password).digest('hex'); const aes = crypto.createCipheriv('aes-256-gcm', key, iv); const encrypted = aes.update(data);` |
| 69 | 测试-单元测试 | ⭐⭐ | [单元测试](https://jestjs.io/) | 单元测试：最小代码单元。框架：Jest/Vitest/Mocha/Chai。原则：AAA(Arrange/Act/Assert)、独立/可重复/快。覆盖率。Mock/Stub/Spy。TDD测试驱动开发。 | `test('add returns sum', () => { expect(add(2,3)).toBe(5); }); test('async fetch', async () => { const data = await fetchUser(1); expect(data.name).toBe('Tom'); });` |
| 70 | 测试-集成测试 | ⭐⭐⭐ | [集成测试](https://testing-library.com/) | 集成测试：多模块交互。API测试：Supertest。数据库：Testcontainers。E2E测试：Playwright/Cypress/Selenium。测试金字塔：单元/集成/E2E比例。测试环境隔离。 | `// API test describe('POST /api/users', () => { it('creates user', async () => { const res = await request(app).post('/api/users').send({name:'Tom'}); expect(res.status).toBe(201); expect(res.body.name).toBe('Tom'); }); });` |
| 71 | 测试-E2E测试 | ⭐⭐⭐⭐ | [E2E测试](https://playwright.dev/) | E2E端到端测试：真实用户场景。工具：Playwright/Cypress。录制/脚本。断言。等待/重试。CI集成。视觉回归测试。移动端测试。 | `test('login flow', async ({ page }) => { await page.goto('/login'); await page.fill('#email', 'test@example.com'); await page.fill('#password', 'password'); await page.click('button[type="submit"]'); await expect(page).toHaveURL('/dashboard'); });` |
| 72 | 设计模式-单例 | ⭐⭐ | [单例模式](https://refactoring.guru/design-patterns/singleton) | 单例模式：类只有一个实例。实现：私有构造函数、静态实例、延迟加载。应用：配置中���、���据库连接、logger。JS中模块即单例。线程安全双检锁。 | `class Singleton { static #instance; static getInstance() { if(!Singleton.#instance) Singleton.#instance = new Singleton(); return Singleton.#instance; } }` |
| 73 | 设计模式-工厂 | ⭐⭐⭐ | [工厂模式](https://refactoring.guru/design-patterns/factory-method) | 工厂模式：封装对象创建。简单工厂、工厂方法、抽象工厂。解耦创建逻辑和使用。应用：组件库、插件系统。 | `class Button { render() { } } class ButtonFactory { static create(type) { switch(type) { case 'primary': return new PrimaryButton(); case 'secondary': return new SecondaryButton(); default: return new Button(); } } }` |
| 74 | 设计模式-观察者 | ⭐⭐⭐ | [观察者模式](https://refactoring.guru/design-patterns/observer) | 观察者模式：发布-订阅。主题维护订阅者列表，状态变化通知。应用：事件系统、响应式编程、消息队列。JS中addEventListener、EventEmitter。 | `class EventEmitter { #events = {}; on(event, fn) { (this.#events[event] ??= []).push(fn); } emit(event, ...args) { this.#events[event]?.forEach(fn => fn(...args)); } }` |
| 75 | 设计模式-策略 | ⭐⭐⭐ | [策略模式](https://refactoring.guru/design-patterns/strategy) | 策略模式：封装算法家族 interchangeable。context持有一个strategy。运行时切换。应用：验证器、排序算法、支付方式。类似简单工厂但context控制。 | `class Sorter { setStrategy(strategy) { this.strategy = strategy; } sort(data) { return this.strategy.sort(data); } } const quickSorter = new Sorter(); quickSorter.setStrategy(new QuickSort());` |
| 76 | 设计模式-装饰器 | ⭐⭐⭐ | [装饰器模式](https://refactoring.guru/design-patterns/decorator) | 装饰器：动态添加功能。替代继承。Python/JavaScript装饰器语法。代码：before/after/around。AOP面向切面编程。日志/缓存/性能监控。 | `function logged(fn) { return (...args) => { console.log('call', fn.name, args); return fn(...args); }; } @logged function add(a,b) { return a+b; }` |
| 77 | 设计模式-代理 | ⭐⭐⭐ | [代理模式](https://refactoring.guru/design-patterns/proxy) | 代理控制对象访问。类型：远程/虚拟/保护/智能引用。应用：懒加载、权限控制、日志、缓存。ES6 Proxy。 | `const proxy = new Proxy(target, { get(obj, prop) { console.log('get', prop); return obj[prop]; }, set(obj, prop, val) { console.log('set', prop, val); obj[prop] = val; } });` |
| 78 | 微服务架构 | ⭐⭐⭐⭐ | [微服务](https://microservices.io/) | 微服务架构：服务独立部署/扩展/维护。通信：REST/gRPC。注册发现：Consul/Eureka。网关：Kong/APISIX。容错：熔断/限流/降级。事务：Saga。配置中心。容器编排K8s。 | `// 服务注册 curl -X POST http://localhost:8500/v1/agent/service/register -d '{"ID":"user","Name":"user","Port":8080}' // HTTP调用 curl http://user-service/api/user/1` |
| 79 | 消息队列 | ⭐⭐⭐⭐ | [消息队列](https://www.rabbitmq.com/) | 消息队列异步通信。生产者/消费者模式。解耦/削峰/可靠。工具：RabbitMQ/Apache Kafka/Redis Stream。交换器：direct/topic/fanout/headers。队列。持久化。 | `// 生产者 channel.basicPublish('', 'queue', Buffer.from(msg)); // 消费者 channel.basicConsume('queue', msg => { console.log(msg.content.toString()); channel.ack(msg); });` |
| 80 | API网关 | ⭐⭐⭐⭐ | [API网关](https://getkong.org/) | API网关：统一入口。路由/负载均衡/认证/限流/日志。Kong/APISIX/Traefik。边缘计算。GraphQL Federation。 | `router.post('/api/:service/:path', async (ctx) => { const token = ctx.headers.authorization; if(!verify(token)) return 401; const service = ctx.params.service; const res = await fetch(\`\http://\${service}/\${ctx.params.path}\`); ctx.body = await res.json(); });` |
| 81 | 配置管理 | ⭐⭐⭐ | [配置](https://12factor.net/config) | 12-Factor配���准���：环境变量存储配置。层级：开发/测试/生产。配置中心：Apollo/Disconf/Nacos。敏感加密。热更新。YAML/TOML。 | `const config = { db: { host: process.env.DB_HOST, port: parseInt(process.env.DB_PORT) }, jwt: { secret: process.env.JWT_SECRET } }; // 12-factor load config from_env()` |
| 82 | 日志系统 | ⭐⭐⭐ | [日志](https://www.elastic.co/cn/logstash) | 日志系统：结构化日志。级别：DEBUG/INFO/WARN/ERROR/FATAL。格式：JSON。输出：Console/File/Stdout。收集：ELK/Loki。链路追踪：Jaeger/Zipkin。采样。 | `logger.debug('request', { method, url, duration: 120 }); logger.info('user', { userId, action: 'login' }); logger.error('error', { error: err.message, stack: err.stack });` |
| 83 | 监控告警 | ⭐⭐⭐⭐ | [监控](https://prometheus.io/) | 监控指标：QPS/Latency/Error率/CPU/Memory/Disk。Prometheus拉取模式。Grafana可视化。告警：Prometheus Alertmanager。APM：SkyWalking/Pinpoint。异常检测。SLO/SLA。 | `// Prometheus counter const requestsTotal = new Counter({ name: 'http_requests_total', help: 'Total requests', labelNames: ['method', 'status'] }); requestsTotal.inc({ method: 'GET', status: '200' }); // Gauge gauge.set('memory_usage', 0.65);` |
| 84 | 无障碍访问(a11y) | ⭐⭐⭐ | [a11y](https://www.w3.org/WAI/fundamentals/accessibility-intro/) | Web无障碍：让残障人士可用。WCAG标准。语义化HTML。ARIA属性。键盘导航。焦点管理。颜色对比度。屏幕阅读器支持。alt文本。 | `<button aria-label="关闭" aria-describedby="desc"><span aria-hidden="true">×</span></button> <a href="#main" class="sr-only">跳到内容</a> <table aria-describedby="caption">` |
| 85 | 国际化i18n | ⭐⭐⭐ | [i18n](https://developer.mozilla.org/zh-CN/docs/Mozilla/Add-ons/WebExtensions/Internationalization) | 国际化：多语言支持。方案：键值对/ICU消息格式。运行时切换。复数/性别/日期/数字格式化。库：i18next/react-intl。静态生成。 | `// en.json {"greeting": "Hello {name}", "items": "{count, plural, =0 {No items} =1 {One item} other {# items}}"} // 使用 t('greeting', { name: 'World' })` |
| 86 | PWA渐进式应用 | ⭐⭐⭐ | [PWA](https://web.dev/articles/progressive-web-apps) | PWA：可安装/离线/推送。清单manifest.json。Service Worker：拦截请求/缓存策略/离线。Web App Manifest。Push API。Background Sync。 | `// manifest.json {"name":"App","short_name":"App","start_url":"/","display":"standalone","icons":[{"src":"icon.png","sizes":"192x192"}]} // SW self.addEventListener('fetch', e => { e.respondWith(caches.match(e.request).then(r=>r\|\|fetch(e.request))); });` |
| 87 | SSR服务端渲染 | ⭐⭐⭐⭐ | [SSR](https://nextjs.org/docs/pages/building-your-application/routing/pages-and-layouts) | SSR服务端渲染：首屏快/SEO好。Next.js/Angular Universal。hydrate水合。流式渲染。边缘渲染。SSG静态生成。ISR增量更新。 | `// Next.js export async function getServerSideProps() { const data = await fetchData(); return { props: { data } }; } // Nuxt.js export default { asyncData({ $axios }) { return { data: await $axios.get('/api') } } }` |
| 88 | CDN与静态资源 | ⭐⭐ | [CDN](https://www.cloudflare.com/learning/cdn/what-is-a-cdn/) | CDN内容分发网络。边缘节点就近访问。缓存策略：Cache-Control/ETag。静态资源(JS/CSS/图片/字体)走CDN。域名分离。Gzip/Brotli压缩。HTTP/2推送。 | `Cache-Control: public, max-age=31536000, immutable <link rel="preconnect" href="https://cdn.example.com">` |
| 89 | OAuth2第三方登录 | ⭐⭐⭐⭐ | [OAuth2](https://oauth.net/2/) | OAuth2第三方认证。流程：Authorization Code Grant。角色：Resource Owner/Client/Authorization Server/Resource Server。Token：Access Token/Refresh Token。Scope范围。常见：Google/GitHub登录。 | `// 1. 跳转授权 const authUrl = 'https://github.com/login/oauth/authorize?client_id=ID&scope=read:user'; // 2. 回调 const code = url.searchParams.get('code'); // 3. 换token const tokenRes = await fetch('https://github.com/login/oauth/access_token', { client_id, client_secret, code });` |
| 90 | WebAuthn无密码登录 | ⭐⭐⭐⭐ | [WebAuthn](https://webauthn.guide/) | WebAuthn：FIDO2标准。公钥凭据。生物识别/安全密钥。服务器存储公钥，浏览器生成签名。防钓鱼。Passkeys。 | `// 注册 const credential = await navigator.credentials.create({ publicKey: { challenge, rp: { name: 'App' }, user: { id: userId, name: userName }, pubKeyCredParams: [{ type: 'public-key', alg: -7 }] } }); // 登录 const assertion = await navigator.credentials.get({ publicKey: { challenge } });` |
| 91 | WebAssembly | ⭐⭐⭐⭐ | [WASM](https://webassembly.org/) | WebAssembly高效运行编译语言。C/C++/Rust/WAT。游戏/视频/图像处理。导入JS。模块化。WASI服务器运行。 | `// Rust编译 rustc --target wasm32-unknown-unknown -O --crate-type=cdylib lib.rs -o lib.wasm // JS import init, { add } from './lib.js'; await init(); console.log(add(1,2));` |
| 92 | 函数式编程 | ⭐⭐⭐⭐ | [函数式](https://mostly-adequate.gitbook.iomostly-adequate-guide/) | 函数式编程：一等函数/纯函数/不可变。map/filter/reduce组合。Currying柯里化。Composition组合。Functor/Applicative/Monad。Pointfree。Lodash/fp。 | `const compose = (...fns) => x => fns.reduceRight((v, f) => f(v), x); const pipe = (...fns) => x => fns.reduce((v, f) => f(v), x); const prop = k => obj => obj[k]; const getName = compose(prop('user'), prop('name'));` |
| 93 | 响应式编程 | ⭐⭐⭐⭐ | [响应式](https://rxjs.dev/) | RxJS响应式编程。Observable观察者模式。操作符：map/filter/mergeMap/switchMap/concatMap。调度器。Hot/Cold Observable。Subject多播。Pipe管道组合。 | `import { fromEvent } from 'rxjs'; import { map, debounceTime, distinctUntilChanged } from 'rxjs/operators'; fromEvent(input, 'input').pipe(debounceTime(300), map(e => e.target.value), distinctUntilChanged()).subscribe(console.log);` |
| 94 | Serverless无服务器 | ⭐⭐⭐⭐ | [Serverless](https://aws.amazon.com/lambda/) | Serverless函数即服务。AWS Lambda/Vercel/Netlify/Cloudflare Workers。触发器：HTTP/API Gateway/定时/S3/队列。按调用计费。冷启动。限制：���时/内存。 | `// Vercel export default async function handler(req, res) { const data = await fetch('https://api.example.com'); return res.json(await data.json()); } // Cloudflare addEventListener('fetch', e => e.respondWith(handle(e)));` |
| 95 | 低代码平台 | ⭐⭐⭐⭐ | [低代码](https://www.mendix.com/the-low-code-guide/) | 低代码：可视化构建应用。OutSystems/Mendix。模型驱动开发。组件库。工作流。集成。快速原型。专业开发扩展。 | `// OutSystems表达式或Mendix微流 // 无代码拖拽设置 // 表达式：$yourvar = $var1 + $var2 // 条件：if $count > 10 then '满' else '不满' endif` |
| 96 | 代码审查 | ⭐⭐⭐ | [Code Review](https://google.github.io/eng-practices/review/) | 代码审查CR。目的：发现bug/保证质量/知识共享。原则：小量多次/描述上下文/客观具体。清单：逻辑/测试/命名/复杂度/注释/风格/重複。CI自动化检查。 | `// PR描述 ## 变更说明 - 添加用户验证 - 修复bug ## 测试 - 单元测试 - 手动测试 // 审查注释 // 问题：这段逻辑重复，请提取为函数。 // 建议：建议用正则简化。` |
| 97 | 技术债务管理 | ⭐⭐⭐⭐ | [技术债务](https://martinfowler.com/bliki/TechnicalDebtQuadrant.html) | 技术债务：代码质量成本。红区/利率高优先处理。记录：TODO/FIXME标签。度量：圈复杂度/重复率/覆盖率。定期重构。预防胜于清理。 | `// TODO: 重构为Promise // FIXME: 内存泄漏 // FIXME(chore): 移除废弃API // 利率高：安全漏洞、性能问题、频繁出错` |
| 98 | 架构设计原则 | ⭐⭐⭐⭐ | [架构](https://www.amazon.com/Architecture-All-Things-Engineers-Practitioners/dp/1492043447) | 架构原则：SOLID/KISS/DRY/YAGNI。分层架构：Controller/Service/Repository。微内核架构。事件溯源。CQRS读写分离。DDD领域驱动设计。 | `// 分层 controller -> service -> repository // DDD聚合根entity // CQRS const cmd = new CommandHandler(userRepo); const q = new QueryHandler(userRO);` |
| 99 | DDD领域驱动设计 | ⭐⭐⭐⭐⭐ | [DDD](https://domainlanguage.com/ddd/) | DDD：领域模型为核心。核心概念：Entity/Value Object/Aggregate/Repository/Service/Domain Event/Bounded Context。战略设计：Ubiquitous Language。战术实现。 | `// Entity class Order { constructor() { this.items = []; } addItem(product, qty) { this.items.push(new OrderItem(product, qty)); } total() { return this.items.reduce((sum,i)=>sum+i.price*i.qty,0); } } // Aggregate根 // Repository interface OrderRepository { find(id); save(order); }` |
| 100 | 全栈项目实战 | ⭐⭐⭐⭐⭐⭐ | [项目](https://fullstackopen.com/) | 全栈项目综合：需求分析/架构设计/前后端实现/测试/部署。完整流程。需求->设计->实现->测试->部署->迭代。团队协作GitFlow。文档。美团/饿了么/博客系统/电商。 | `# 全栈项目Todo 1. 需求分析 2. 技术选型 3. API设计 4. 前端开发 5. 后端开发 6. 单元测试 7. 集成测试 8. 部署DevOps 9. 监控日志 10. 迭代优化` |

---

*Generated by 小花 @ 2026-03-30 | 100个知识点完整表格*