{
  "version": 2,
  "buildCommand": "echo 'static site'",
  "installCommand": "echo 'no deps'",
  "framework": null,
  "outputDirectory": "pages",
  "routes": [
    { "src": "/data/(.*)", "dest": "/data/$1" },
    { "src": "/(.*)", "dest": "/pages/$1" }
  ]
}
